<Window x:Class="View.NoteView2"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:View"
        mc:Ignorable="d"
        Title="NoteView2" Height="1000" Width="1000">
    <Grid>
        <Grid Name="TopGrid" Margin="0,0,0,583">


            <Label Margin="33,0,909,346" Content="{Binding ParagraphNumber}" ></Label>
            <Label Margin="0,0,959,346" Content="§ Nr." />
            <TextBlock Margin="72,0,253,346" Name="TextBlockHeadLine" DataContext="{Binding HeadLine}"></TextBlock>
            <TextBlock Name="TextBlockLawtext" DataContext="{Binding LawText}" Margin="0,40,253,200"></TextBlock>
        </Grid>
        <Grid Margin="0,186,252,202" Name="GridNote">
            <Border BorderBrush="Black" Name="BorderTextField" BorderThickness="1" HorizontalAlignment="Left" Height="581" VerticalAlignment="Top" Width="740"/>
            <RichTextBox Name="NoteRichTextBox" DataContext="{Binding NoteText}" AcceptsTab="True"  >
                <FlowDocument>
                    <Paragraph>
                        
                    </Paragraph>
                </FlowDocument>
            </RichTextBox>
            </Grid>
    </Grid>


</Window>
